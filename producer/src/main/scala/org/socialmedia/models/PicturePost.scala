package org.socialmedia.models

case class PicturePost(
                          pictureId: Int,
                          publisherId: Int,
                          timestamp: Long
                        )
