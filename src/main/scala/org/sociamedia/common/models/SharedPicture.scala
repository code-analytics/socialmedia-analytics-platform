package org.sociamedia.common.models

case class SharedPicture(
                          pictureId: Int,
                          publisherId: Int,
                          pictureDate: Long,
                          pictureDuration: Int
                        )
