package org.sociamedia.common.models

case class PicturePost(
                          pictureId: Int,
                          publisherId: Int,
                          pictureDate: Long
                        )
