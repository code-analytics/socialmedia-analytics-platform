package org.sociamedia.common.models

case class VideoPost(
                        videoId: Int,
                        publisherId: Int,
                        videoDate: Long,
                        videoDuration: Int
                      )
